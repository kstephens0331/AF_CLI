export default function Page() {
  return (
    <main>
      <h1>AF CLI – API endpoints</h1>
      <p>Next.js shell so Vercel can build. API routes live under <code>app/api/*</code>.</p>
    </main>
  );
}